# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/
*.iml

# Java / Maven
**/target/
*.log

# LaTeX (intermedios; se conserva el .pdf como entregable)
*.aux
*.toc
*.out
*.lof
*.lot
*.synctex.gz
*.fls
*.fdb_latexmk

# Env / secrets
.env
.env.*
!.env.example
# Scripts locales con credenciales (no subir al repo)
*.local.ps1
# Volcados de base de datos
/backups/

# Cursor local artifacts
.cursor/

# Config local de servidores MCP (apunta a puertos de la maquina, no es del proyecto)
.mcp.json

# OS / editor extra
*.swp
*~
desktop.ini
*.dia~

# Copia local de docs retirados del repo (datos personales / notas internas)
/_private/
